@page
@model EtlTool.App.Pages.Account.SqliteBackupModel
@{
    Layout = null;
}
@*
    This page is a no-render fallback. The handler streams the backup file
    directly via OnGetAsync. If the user navigates here without the handler
    parameter, we redirect to /system.
*@
